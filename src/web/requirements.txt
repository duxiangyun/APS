# ============================================================
# APS 智能排产系统依赖（版本已锁定，与开发环境一致）
# 安装: pip install -r requirements.txt
# 说明: 锁定精确版本(==)以避免本地与服务器环境差异导致的兼容问题
# ============================================================

# ---------- Web 框架 ----------
fastapi==0.115.0
starlette==0.38.6
pydantic==2.12.5
pydantic-core==2.41.5
anyio==4.12.1
sniffio==1.3.1
typing-extensions==4.15.0

# ---------- ASGI 服务器 ----------
uvicorn[standard]==0.32.0
click==8.3.1
h11==0.16.0
httptools==0.7.1
python-dotenv==1.2.1
PyYAML==6.0.3
watchfiles==1.1.1
websockets==16.0
uvloop==0.22.1; sys_platform != "win32"

# ---------- 模板引擎 ----------
jinja2==3.1.6
MarkupSafe==3.0.3

# ---------- 表单解析 ----------
python-multipart==0.0.12

# ---------- 求解器与数值计算（排产算法子进程使用）----------
gurobipy==12.0.3
highspy==1.14.0
numpy==2.4.2
